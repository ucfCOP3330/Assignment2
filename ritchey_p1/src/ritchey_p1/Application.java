package ritchey_p1;

import javax.swing.JOptionPane;
import ritchey_p1.Encrypter;
import ritchey_p1.Decrypter;
//I could not get the application to work, my encrypt and decrypt files work correctly on their own


@SuppressWarnings("unused")
public class Application {
	
	public static void main(String[] args)
	{
 //declaration of variables to be used in calculations
	/*	String fourDigitNum;
		int numOne;
		int firstDigit = 0, secondDigit = 0, thirdDigit = 0, fourthDigit = 0, decryptNum;
 //i need to get input from the user
		
					
			fourDigitNum = JOptionPane.showInputDialog("Enter a 4 digit number");
			numOne = Integer.parseInt(fourDigitNum);
			String EncryptedNum = Encrypter.Encrypt(numOne);
			Decrypter.Decrypt(EncryptedNum);
		System.out.println( "Your number encrypted is " + firstDigit + secondDigit + thirdDigit + fourthDigit);
	*/
	}
	
}
